# `cargo xtask check` is the specification gate. Without this alias it answers
# `no such command: xtask`, which reads like a typo and gets stepped past.
[alias]
xtask = "run -q --locked -p xtask --"
