---
"@origin89/km43": patch
---

Doc comments only. A member the registry names without a meaning, such as `Firmware.Accepted`, now shows that name as code. `Suite.X25519ChachapolySha256` and `LinkErrorCode.BeforeLinkUp` quote the identifiers they mention, and `Quality.Estimated` spells out *state of charge*. No value, name or type changed.
