---
"@origin89/km43": minor
---

Add `BehaviourKey`, whose `Shadow` member is the key every behaviour config section carries its `shadow` flag under. `ConfigSection.IdentityAndSite` and `ConfigSection.Network` are no longer marked reserved: their section bodies are now defined.
