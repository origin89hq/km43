---
"@origin89/km43": minor
---

`BootReason` follows what the controller can tell apart: `PowerOn` becomes
`Power`, `BrownOut` is retired, and `PinReset`, `OptionByteReload`,
`WindowWatchdog` and `LowPowerEntry` are added.
