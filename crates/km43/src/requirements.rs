// Generated by `cargo xtask registry` from docs/PROTOCOL.md and docs/protocol/LINK.md.
// Do not edit: a rule is numbered in its document, and this table follows it.

use crate::Requirement;

/// Every numbered rule of KM43, in document order: PROTOCOL.md, then LINK.md.
pub const REQUIREMENTS: &[Requirement] = &[
    Requirement {
        id: "P-001",
        document: "docs/PROTOCOL.md",
        section: "How to read this",
        statement: "An implementation MUST reproduce every vector in `protocol/vectors/v1.json` applicable to its transports exactly before it is connected to another implementation.",
    },
    Requirement {
        id: "P-005",
        document: "docs/PROTOCOL.md",
        section: "Reported by the controller",
        statement: "The controller MUST report keys 12 to 17 with the values it actually enforces, and a client MUST use the reported value in place of any it was built with.",
    },
    Requirement {
        id: "P-006",
        document: "docs/PROTOCOL.md",
        section: "Reported by the controller",
        statement: "`max_channels` (key 13) MUST only ever be reported **downward from 32**.",
    },
    Requirement {
        id: "P-002",
        document: "docs/PROTOCOL.md",
        section: "Neither fixed nor reported",
        statement: "A receiver MUST size its frame buffer at `MAX_FRAME` and MUST discard, without allocating, any frame that would exceed it.",
    },
    Requirement {
        id: "P-003",
        document: "docs/PROTOCOL.md",
        section: "Neither fixed nor reported",
        statement: "Refusing MUST be preferred to evicting.",
    },
    Requirement {
        id: "P-169",
        document: "docs/PROTOCOL.md",
        section: "Neither fixed nor reported",
        statement: "The concern table MUST refuse rather than evict, MUST reserve `MAX_CONCERNS − MAX_CONCERNS_BELOW_FAULT` rows for `fault` and `protection`, and MUST report the count it has refused in `Concerns` key 6.",
    },
    Requirement {
        id: "P-004",
        document: "docs/PROTOCOL.md",
        section: "Neither fixed nor reported",
        statement: "Every duration in this document MUST be measured on a **monotonic tick since boot**, never on the wall clock: the 15-minute session expiry, the 120-second challenge and pairing windows, `MAX_CMD_DEDUP`'s 10 minutes, the 60-second `MAX_AUTH_FAILURES` window, the 5-second BLE reassembly timeout, the 50 ms incomplete-frame timeout, and every other one below.",
    },
    Requirement {
        id: "P-010",
        document: "docs/PROTOCOL.md",
        section: "Encoding",
        statement: "Envelopes MUST be CBOR arrays.",
    },
    Requirement {
        id: "P-011",
        document: "docs/PROTOCOL.md",
        section: "Encoding",
        statement: "Bodies MUST be CBOR maps with **integer keys only**.",
    },
    Requirement {
        id: "P-012",
        document: "docs/PROTOCOL.md",
        section: "Encoding",
        statement: "A retired field number MUST NOT be reused.",
    },
    Requirement {
        id: "P-013",
        document: "docs/PROTOCOL.md",
        section: "Encoding",
        statement: "A decoder MUST skip map keys it does not recognise, and MUST NOT reject a message for containing them.",
    },
    Requirement {
        id: "P-014",
        document: "docs/PROTOCOL.md",
        section: "Encoding",
        statement: "A decoder MUST reject an **enum discriminant** it does not recognise, with error 1.",
    },
    Requirement {
        id: "P-019",
        document: "docs/PROTOCOL.md",
        section: "Encoding",
        statement: "P-014 has exactly one exception: the vendor and experimental range `0xF000`–`0xFFFF`, reserved by [REGISTRY.md](protocol/REGISTRY.md) in the **metric-kind and event-kind spaces only** and never allocated by this document.",
    },
    Requirement {
        id: "P-015",
        document: "docs/PROTOCOL.md",
        section: "Encoding",
        statement: "Every key listed in a body definition below is REQUIRED unless marked *optional*.",
    },
    Requirement {
        id: "P-016",
        document: "docs/PROTOCOL.md",
        section: "Encoding",
        statement: "Encoders MUST emit RFC 8949 §4.2 deterministic encoding: shortest form integers, sorted map keys, definite lengths, no indefinite strings.",
    },
    Requirement {
        id: "P-017",
        document: "docs/PROTOCOL.md",
        section: "Encoding",
        statement: "Authentication MUST NOT depend on P-016.",
    },
    Requirement {
        id: "P-018",
        document: "docs/PROTOCOL.md",
        section: "Encoding",
        statement: "Floating point MUST NOT appear on the wire.",
    },
    Requirement {
        id: "P-028",
        document: "docs/PROTOCOL.md",
        section: "Envelope",
        statement: "The **array length is the extension point**.",
    },
    Requirement {
        id: "P-029",
        document: "docs/PROTOCOL.md",
        section: "Envelope",
        statement: "Every `seq` bound in this document names the **first position included**.",
    },
    Requirement {
        id: "P-020",
        document: "docs/PROTOCOL.md",
        section: "Envelope",
        statement: "`type` with the high bit set means *response to a request*.",
    },
    Requirement {
        id: "P-021",
        document: "docs/PROTOCOL.md",
        section: "Envelope",
        statement: "A client that has no session yet MUST send `session_id = 0`, and MUST NOT rely on that value surviving.",
    },
    Requirement {
        id: "P-026",
        document: "docs/PROTOCOL.md",
        section: "Envelope",
        statement: "The controller MUST answer a pre-session request — `Discover 0x80`, `Pair 0x8B`, `Enrol 0x93`, `Hello 0x81`, and any bare `Error` answering one — with the connection handle in `session_id`, so the response routes back to the connection that asked.",
    },
    Requirement {
        id: "P-022",
        document: "docs/PROTOCOL.md",
        section: "Envelope",
        statement: "`req_id` is a **`u32`**, MUST be **strictly increasing within a session**, and MUST NOT be reused or restarted.",
    },
    Requirement {
        id: "P-023",
        document: "docs/PROTOCOL.md",
        section: "Envelope",
        statement: "For an unsolicited `Event` the sender MUST set `req_id = 0`, and it enters the associated data as `0x00000000` — four zero bytes, the same width every other `req_id` occupies (P-234).",
    },
    Requirement {
        id: "P-024",
        document: "docs/PROTOCOL.md",
        section: "Envelope",
        statement: "A client MUST drop a response whose `(session_id, req_id)` matches no outstanding request.",
    },
    Requirement {
        id: "P-025",
        document: "docs/PROTOCOL.md",
        section: "Envelope",
        statement: "A frame too malformed to parse its envelope MUST be answered with `session_id = 0, req_id = 0`, and the comms processor MUST route that answer back on the connection the bad frame arrived on.",
    },
    Requirement {
        id: "P-027",
        document: "docs/PROTOCOL.md",
        section: "Envelope",
        statement: "An `Error` answering a frame whose envelope parsed MUST echo that frame's `session_id` and `req_id`, and those echoed values are what enter the associated data of a sealed one (P-234).",
    },
    Requirement {
        id: "P-030",
        document: "docs/PROTOCOL.md",
        section: "UART — controller ↔ comms processor",
        statement: "A receiver MUST resynchronise by reading to the next `0x00`.",
    },
    Requirement {
        id: "P-031",
        document: "docs/PROTOCOL.md",
        section: "UART — controller ↔ comms processor",
        statement: "A frame failing its CRC MUST be dropped silently.",
    },
    Requirement {
        id: "P-032",
        document: "docs/PROTOCOL.md",
        section: "UART — controller ↔ comms processor",
        statement: "A COBS code byte of `0x00` inside a frame, or a block running past the end of a frame, MUST be treated as corruption: drop and resynchronise.",
    },
    Requirement {
        id: "P-033",
        document: "docs/PROTOCOL.md",
        section: "UART — controller ↔ comms processor",
        statement: "RTS/CTS is REQUIRED at this rate and the production connector MUST carry both signals.",
    },
    Requirement {
        id: "P-034",
        document: "docs/PROTOCOL.md",
        section: "WebSocket",
        statement: "One protocol message per binary WebSocket frame.",
    },
    Requirement {
        id: "P-223",
        document: "docs/PROTOCOL.md",
        section: "WebSocket",
        statement: "The comms processor MUST accept the RFC 6455 opening handshake on TCP port `WS_PORT` (80) with the request-target `WS_PATH` (`/km43`), and MUST answer any other request-target with HTTP 404 and no upgrade.",
    },
    Requirement {
        id: "P-224",
        document: "docs/PROTOCOL.md",
        section: "WebSocket",
        statement: "While the station holds an IPv4 address, the comms processor MUST answer multicast DNS (RFC 6762) for its host name with the address `WifiStatus` key 5 reports, and MUST advertise exactly one DNS-SD (RFC 6763) instance of type `DNSSD_SERVICE` (`_km43._tcp`) in `local.`.",
    },
    Requirement {
        id: "P-225",
        document: "docs/PROTOCOL.md",
        section: "WebSocket",
        statement: "A client MUST run `Discover` on every candidate address before using it, whether a discovered instance or a remembered `WifiStatus` address, and MUST apply P-222 to the answer.",
    },
    Requirement {
        id: "P-035",
        document: "docs/PROTOCOL.md",
        section: "USB CDC",
        statement: "As UART, including COBS and the CRC, without the flow-control requirement.",
    },
    Requirement {
        id: "P-036",
        document: "docs/PROTOCOL.md",
        section: "BLE GATT",
        statement: "`flags` bit 7 (`BLE_LAST_FLAG`, `0x80`) is `last`; bits 6–0 (`BLE_INDEX_MASK`) are the fragment index, starting at zero and increasing by one through at most 127.",
    },
    Requirement {
        id: "P-037",
        document: "docs/PROTOCOL.md",
        section: "BLE GATT",
        statement: "Fragment data MUST be at most `min(ATT_MTU − 3, 512) − 2` bytes: three ATT bytes, then two KM43 bytes, with the GATT attribute limit also applied.",
    },
    Requirement {
        id: "P-039",
        document: "docs/PROTOCOL.md",
        section: "BLE GATT",
        statement: "`msg_id` identifies the protocol message a fragment belongs to.",
    },
    Requirement {
        id: "P-038",
        document: "docs/PROTOCOL.md",
        section: "MQTT",
        statement: "`<device_id>` MUST be the 16 bytes rendered as 32 **lowercase** hexadecimal characters, no separators.",
    },
    Requirement {
        id: "P-040",
        document: "docs/PROTOCOL.md",
        section: "Cryptographic conventions",
        statement: "Every integer entering a MAC, a KDF, a prologue or an associated data string MUST be encoded **big-endian, fixed width, with no padding and no length prefix**.",
    },
    Requirement {
        id: "P-041",
        document: "docs/PROTOCOL.md",
        section: "Cryptographic conventions",
        statement: "`HMAC` means **HMAC-SHA256**.",
    },
    Requirement {
        id: "P-042",
        document: "docs/PROTOCOL.md",
        section: "Cryptographic conventions",
        statement: "`HKDF` means **HKDF-SHA256** (RFC 5869) with `salt`, `IKM` and `info` as **named arguments**.",
    },
    Requirement {
        id: "P-043",
        document: "docs/PROTOCOL.md",
        section: "Cryptographic conventions",
        statement: "Every **domain input** MUST begin with its label, so a value computed for one purpose can never verify for another.",
    },
    Requirement {
        id: "P-226",
        document: "docs/PROTOCOL.md",
        section: "Cryptographic conventions",
        statement: "Key agreement and session encryption are Noise ([noiseprotocol.org](https://noiseprotocol.org/noise.html), revision 34) with **suite 1**: X25519, ChaCha20-Poly1305 and SHA-256.",
    },
    Requirement {
        id: "P-228",
        document: "docs/PROTOCOL.md",
        section: "Cryptographic conventions",
        statement: "Every X25519 operation, on either side, MUST refuse an all-zero output and abandon the handshake.",
    },
    Requirement {
        id: "P-235",
        document: "docs/PROTOCOL.md",
        section: "Keys",
        statement: "The controller key MUST be generated at manufacture from a CSPRNG, MUST stay the same for the life of the unit, and MUST NOT be changed by a factory reset.",
    },
    Requirement {
        id: "P-236",
        document: "docs/PROTOCOL.md",
        section: "Keys",
        statement: "The controller key's fingerprint is `controller_fp = SHA-256(\"km43/v1/controller-fp\" | CS)[0..16]` and it is printed on the label (P-049).",
    },
    Requirement {
        id: "P-237",
        document: "docs/PROTOCOL.md",
        section: "Keys",
        statement: "Every random value the controller uses — each ephemeral key and each challenge — MUST come from a deterministic random bit generator whose state: - is generated at manufacture from a CSPRNG and is never derived from, or recoverable from, `printed_secret`, `device_id` or anything the controller transmits; - is advanced irreversibly on every draw, and the advanced state is persisted and read back **before** the draw is used; - is never re-initialised: not by a factory reset, not by recovering a corrupt store, not by a firmware update.",
    },
    Requirement {
        id: "P-044",
        document: "docs/PROTOCOL.md",
        section: "Keys",
        statement: "`printed_secret` MUST be exactly **32 bytes of entropy**, carried in the QR code as 64 lowercase hexadecimal characters.",
    },
    Requirement {
        id: "P-088",
        document: "docs/PROTOCOL.md",
        section: "Keys",
        statement: "`printed_secret` MUST derive exactly two keys and MUST NOT be used directly as a key: `pair_psk = HKDF(salt = device_id, 16 bytes ikm = printed_secret, 32 bytes info = \"km43/v1/pair-psk\", L = 32) refusal_key = HKDF(salt = device_id, ikm = printed_secret, info = \"km43/v1/pair-refusal\", L = 32)` Both are used only by a pairing (P-066, P-241).",
    },
    Requirement {
        id: "P-045",
        document: "docs/PROTOCOL.md",
        section: "Keys",
        statement: "No private key and no shared secret is ever transmitted.",
    },
    Requirement {
        id: "P-085",
        document: "docs/PROTOCOL.md",
        section: "Keys",
        statement: "`epoch` is a `u32` monotonic counter in FRAM.",
    },
    Requirement {
        id: "P-086",
        document: "docs/PROTOCOL.md",
        section: "Keys",
        statement: "`client_id` MUST be allocated as the **lowest free slot index in the client table, counting from 1**, whenever P-240 or P-255 allocates a free slot.",
    },
    Requirement {
        id: "P-087",
        document: "docs/PROTOCOL.md",
        section: "Keys",
        statement: "`Discover 0x80` MUST carry the current `epoch` (key 8).",
    },
    Requirement {
        id: "P-222",
        document: "docs/PROTOCOL.md",
        section: "Keys",
        statement: "A client that keeps its enrolment across a restart MUST keep its own static key `is`, the controller key `CS` it pinned, the `device_id` and the suite, and MAY keep the `client_id`, generation and epoch it was told.",
    },
    Requirement {
        id: "P-049",
        document: "docs/PROTOCOL.md",
        section: "Keys",
        statement: "The QR code payload MUST be exactly `km43:2:<device_id>:<printed_secret>:<controller_fp>` — the literal ASCII `km43`, a colon, the payload version `2`, a colon, the `device_id` as the 32 lowercase hexadecimal characters of P-038, a colon, the `printed_secret` as the 64 lowercase hexadecimal characters of P-044, a colon, and the `controller_fp` of P-236 as 32 lowercase hexadecimal characters.",
    },
    Requirement {
        id: "P-227",
        document: "docs/PROTOCOL.md",
        section: "The prologue",
        statement: "Both handshakes use this prologue: `prologue = \"km43/v1/prologue\" | suite:u8 | protocol_major:u8 | protocol_minor:u8 | device_id[16] | epoch:u32be | challenge[16] | handle:u16be` The client takes `protocol_major`, `protocol_minor`, `device_id` and `epoch` from the most recent `Discover 0x80` on this connection, `challenge` from whichever live challenge it is presenting — `Discover` key 7, or `Enrol 0x93` key 4 after an enrolment on this connection — and `handle` from the `session_id` its pre-session responses carry (P-024).",
    },
    Requirement {
        id: "P-230",
        document: "docs/PROTOCOL.md",
        section: "Session encryption",
        statement: "When a handshake completes, `Split()` gives two keys: the first seals what the client sends, the second what the controller sends.",
    },
    Requirement {
        id: "P-231",
        document: "docs/PROTOCOL.md",
        section: "Session encryption",
        statement: "Every message after a handshake, except a bare `Error` (P-142), MUST carry exactly this body: `sealed body of a request 1: sealed bstr ciphertext | tag16; the nonce is the envelope's req_id sealed body of a response or an event 1: sealed bstr ciphertext | tag16 2: nonce u64 the controller's nonce in this direction` `sealed` is the ChaCha20-Poly1305 encryption of the inner body under the direction's key, the nonce below and the associated data of P-234, with the 16-byte tag appended.",
    },
    Requirement {
        id: "P-232",
        document: "docs/PROTOCOL.md",
        section: "Session encryption",
        statement: "A nonce MUST be used once per key, and the sealing side, never its caller, picks it: - A request's nonce is its `req_id` (P-022). The client's sealing state issues `req_id` values itself, starting at 1 and rising by one per request, so a `req_id` that was never sealed cannot be put on the wire and one that was cannot be put there twice. - The controller's nonce starts at 0 and rises by one per message it seals, response or event alike. - The ChaCha20-Poly1305 nonce is four zero bytes followed by the nonce as a little-endian `u64`, which is Noise's layout. - A session MUST end before a `req_id` would pass `2^32 − 1` or a controller nonce would reach `2^64 − 1`.",
    },
    Requirement {
        id: "P-233",
        document: "docs/PROTOCOL.md",
        section: "Session encryption",
        statement: "A receiver MUST verify the tag before reading anything inside, and MUST discard the message on failure (P-051).",
    },
    Requirement {
        id: "P-234",
        document: "docs/PROTOCOL.md",
        section: "Session encryption",
        statement: "The associated data of every sealed message is `ad = type:u8 | session_id:u16be | req_id:u32be` — the three envelope scalars, which are exactly what the comms processor routes on.",
    },
    Requirement {
        id: "P-046",
        document: "docs/PROTOCOL.md",
        section: "Session encryption",
        statement: "`type` MUST be inside every associated data string, so a sealed `SetConfig` cannot be replayed as a sealed `Command`.",
    },
    Requirement {
        id: "P-047",
        document: "docs/PROTOCOL.md",
        section: "Session encryption",
        statement: "`req_id` MUST be inside the associated data in both directions.",
    },
    Requirement {
        id: "P-048",
        document: "docs/PROTOCOL.md",
        section: "Session encryption",
        statement: "A write's operation body **is** its sealed inner body, and a receiver decodes it only after the body has opened.",
    },
    Requirement {
        id: "P-050",
        document: "docs/PROTOCOL.md",
        section: "Sealed bodies",
        statement: "Every message listed in P-052 wears the sealed body of P-231, and nothing outside `sealed` is interpreted before the tag verifies: a `nonce` is checked against the window (P-233) and used as the nonce, and that is all.",
    },
    Requirement {
        id: "P-051",
        document: "docs/PROTOCOL.md",
        section: "Sealed bodies",
        statement: "For every message in P-052 a receiver MUST verify the tag **before** decoding the inner body, and MUST discard the message on failure.",
    },
    Requirement {
        id: "P-052",
        document: "docs/PROTOCOL.md",
        section: "Sealed bodies",
        statement: "The following wear the sealed body under the session's keys: | Messages | Sealed by | |---|---| | Requests `0x03`, `0x05`, `0x06`, `0x07`, `0x08`, `0x09`, `0x0A`, `0x0C`, `0x0D`, `0x0E`, `0x0F`, `0x10`, `0x11`, `0x12`, `0x15`, `0x16`, `0x17`, `0x18` | the client | | Responses `0x83`, `0x85`, `0x86`, `0x87`, `0x88`, `0x89`, `0x8A`, `0x8C`, `0x8D`, `0x8E`, `0x8F`, `0x90`, `0x91`, `0x92`, `0x95`, `0x96`, `0x97`, `0x98`, and `0xFF` in its sealed form (P-142) | the controller | | Event `0x04` | the controller | | `Enrol 0x93` | the controller, under the keys of the pairing that just completed (P-064) | Every other message is `Discover` or a handshake message, and P-054 says what authenticates each.",
    },
    Requirement {
        id: "P-053",
        document: "docs/PROTOCOL.md",
        section: "Sealed bodies",
        statement: "Signed requests (`0x07`, `0x08`, `0x09`, `0x0A`, `0x15`, `0x16`, `0x17`) are sealed like every other request, and their inner body is their operation body (P-080).",
    },
    Requirement {
        id: "P-054",
        document: "docs/PROTOCOL.md",
        section: "Sealed bodies",
        statement: "`0x00`/`0x80` (Discover) are **unauthenticated**: no key exists yet.",
    },
    Requirement {
        id: "P-055",
        document: "docs/PROTOCOL.md",
        section: "Sealed bodies",
        statement: "A client MUST NOT render an unauthenticated message as a statement about the site.",
    },
    Requirement {
        id: "P-056",
        document: "docs/PROTOCOL.md",
        section: "Sealed bodies",
        statement: "A client MUST hold a mark meaning **the lowest `seq` it will still accept**, and MUST reject an unsolicited `Event` (`0x04`) whose `seq` is below it.",
    },
    Requirement {
        id: "P-057",
        document: "docs/PROTOCOL.md",
        section: "Sealed bodies",
        statement: "A handshake message is read in the order its pattern writes it, and nothing it carries is acted on before the step that authenticates it: - `Pair 0x0B`: the suite, then message 1, which opens only under `pair_psk`; only then its payload. - `Enrol 0x13`: message 3, which opens only for the peer that holds the static key inside it; only then is a slot written. - `Hello 0x01`: the suite, then the admission tag (P-238), which selects a slot; then `es` and the client key, which must be that slot's; then `ss`, which is what proves the sender holds it; only then the payload.",
    },
    Requirement {
        id: "P-060",
        document: "docs/PROTOCOL.md",
        section: "Discover — `0x00` / `0x80`",
        statement: "The controller MUST attempt to mint a **fresh challenge per connection**, using the connection handle from [LINK.md](protocol/LINK.md) L-060, and MUST hold at most `MAX_CHALLENGES`.",
    },
    Requirement {
        id: "P-061",
        document: "docs/PROTOCOL.md",
        section: "Discover — `0x00` / `0x80`",
        statement: "A challenge MUST be **single-use**: consumed by the first `Pair` or `Hello` that presents it, whether or not the handshake succeeds.",
    },
    Requirement {
        id: "P-062",
        document: "docs/PROTOCOL.md",
        section: "Discover — `0x00` / `0x80`",
        statement: "A challenge MUST expire 120 seconds after it is minted, and MUST be discarded when its connection drops.",
    },
    Requirement {
        id: "P-063",
        document: "docs/PROTOCOL.md",
        section: "Discover — `0x00` / `0x80`",
        statement: "A challenge MUST be a draw from the controller's random bit generator (P-237).",
    },
    Requirement {
        id: "P-229",
        document: "docs/PROTOCOL.md",
        section: "Pair — `0x0B` / `0x8B`, and Enrol — `0x13` / `0x93`",
        statement: "A connection MUST hold at most one pairing handshake, and a new `Pair 0x0B` abandons the one it held.",
    },
    Requirement {
        id: "P-066",
        document: "docs/PROTOCOL.md",
        section: "Pair — `0x0B` / `0x8B`, and Enrol — `0x13` / `0x93`",
        statement: "A physical act at the controller MUST gate enrolment under the label: the pushbutton on a board that has one, otherwise the selector gesture the controller's design defines under *Auto / off / manual* ([CONTROLLER-V1](https://github.com/origin89hq/origin89/blob/main/docs/CONTROLLER-V1.md)), with the first-enrolment power-on exception below.",
    },
    Requirement {
        id: "P-241",
        document: "docs/PROTOCOL.md",
        section: "Pair — `0x0B` / `0x8B`, and Enrol — `0x13` / `0x93`",
        statement: "When message 1 opens and the window is closed, the controller MUST answer `Pair 0x8B` with outcome 2 `window_closed`; when there is no free slot and no slot with this `label`, with outcome 4 `table_full`.",
    },
    Requirement {
        id: "P-058",
        document: "docs/PROTOCOL.md",
        section: "Pair — `0x0B` / `0x8B`, and Enrol — `0x13` / `0x93`",
        statement: "`Enrol 0x93` MUST carry `next_challenge`: the challenge the controller mints for that connection, minted under P-060 and P-063 like any other, on **every** outcome.",
    },
    Requirement {
        id: "P-064",
        document: "docs/PROTOCOL.md",
        section: "Pair — `0x0B` / `0x8B`, and Enrol — `0x13` / `0x93`",
        statement: "Message 2 goes only to a peer whose message 1 opened, while the window is open and P-240's step 2 or 3 would allocate, which is the condition P-241 refuses on, and carries outcome 6 `proceed`.",
    },
    Requirement {
        id: "P-242",
        document: "docs/PROTOCOL.md",
        section: "Pair — `0x0B` / `0x8B`, and Enrol — `0x13` / `0x93`",
        statement: "A client that sent `Enrol 0x13` and received no `Enrol 0x93` MUST NOT assume it was refused.",
    },
    Requirement {
        id: "P-067",
        document: "docs/PROTOCOL.md",
        section: "Pair — `0x0B` / `0x8B`, and Enrol — `0x13` / `0x93`",
        statement: "A full client table MUST refuse with outcome 4 when P-240 finds nothing to allocate.",
    },
    Requirement {
        id: "P-068",
        document: "docs/PROTOCOL.md",
        section: "Pair — `0x0B` / `0x8B`, and Enrol — `0x13` / `0x93`",
        statement: "Recovering an enrolment is a **re-pair, not a request**.",
    },
    Requirement {
        id: "P-240",
        document: "docs/PROTOCOL.md",
        section: "Pair — `0x0B` / `0x8B`, and Enrol — `0x13` / `0x93`",
        statement: "Allocation MUST run in this order, and MUST compare `label` as the exact UTF-8 bytes `PairOffer` carried, with no case folding, trimming or normalisation: 1. the slot that already holds this client key: the same install pairing again; 2. otherwise the lowest free slot (P-086), answered outcome 1 `enrolled`; 3. otherwise the lowest occupied slot whose `label` is byte-identical, re-keyed and answered outcome 5 `reclaimed`; 4. otherwise outcome 4 `table_full`.",
    },
    Requirement {
        id: "P-078",
        document: "docs/PROTOCOL.md",
        section: "Pair — `0x0B` / `0x8B`, and Enrol — `0x13` / `0x93`",
        statement: "The client table exists to be spent: `MAX_CLIENTS` is shared by every phone, browser, CLI and cloud client, and a reinstalled app, a replaced phone or a cleared browser each spends a slot.",
    },
    Requirement {
        id: "P-239",
        document: "docs/PROTOCOL.md",
        section: "Pair — `0x0B` / `0x8B`, and Enrol — `0x13` / `0x93`",
        statement: "A slot's **key record** holds its state, the epoch it was written in, its generation, its suite, the client's static key, its admission key (P-238), its role (P-250), the `client_kind` and the `label`.",
    },
    Requirement {
        id: "P-105",
        document: "docs/PROTOCOL.md",
        section: "Pair — `0x0B` / `0x8B`, and Enrol — `0x13` / `0x93`",
        statement: "Every enrolled client MUST carry a **capability mask**, and it MUST be the row of [REGISTRY.md](protocol/REGISTRY.md) for the slot's role (P-250) and nothing else.",
    },
    Requirement {
        id: "P-238",
        document: "docs/PROTOCOL.md",
        section: "Hello — `0x01` / `0x81`",
        statement: "Each slot holds an admission key, and each `Hello` carries a tag under it: `admit_key = HKDF(salt = device_id, ikm = X25519(is, CS), info = \"km43/v1/admit-key\", L = 32) admit = HMAC(admit_key, \"km43/v1/hello-admit\" | prologue | handshake)[0..16]` The `ikm` is one value computed from either end: the client computes `X25519(is, CS)`, and the controller the equal `X25519(cs, IS)`.",
    },
    Requirement {
        id: "P-070",
        document: "docs/PROTOCOL.md",
        section: "Hello — `0x01` / `0x81`",
        statement: "`HelloOffer` MUST be the payload of message 1, so that `protocol_major`, `protocol_minor` and `client_version` are authenticated by `ss` and cannot be rewritten in flight.",
    },
    Requirement {
        id: "P-071",
        document: "docs/PROTOCOL.md",
        section: "Hello — `0x01` / `0x81`",
        statement: "The client's ephemeral key MUST come from a CSPRNG and MUST be fresh per handshake.",
    },
    Requirement {
        id: "P-072",
        document: "docs/PROTOCOL.md",
        section: "Hello — `0x01` / `0x81`",
        statement: "The `session_id` is the connection handle, and it is inside the prologue (P-227): a client that builds the prologue with a handle the comms processor rewrote gets a message 2 that does not open.",
    },
    Requirement {
        id: "P-073",
        document: "docs/PROTOCOL.md",
        section: "Hello — `0x01` / `0x81`",
        statement: "A major version mismatch MUST refuse the session with error 3.",
    },
    Requirement {
        id: "P-074",
        document: "docs/PROTOCOL.md",
        section: "Hello — `0x01` / `0x81`",
        statement: "`state_seq` is the state store's own counter.",
    },
    Requirement {
        id: "P-075",
        document: "docs/PROTOCOL.md",
        section: "Hello — `0x01` / `0x81`",
        statement: "A client SHOULD refuse, and MUST surface, a session whose `log_newest_seq` or `state_seq` has regressed below the highest it previously accepted from that `device_id`.",
    },
    Requirement {
        id: "P-243",
        document: "docs/PROTOCOL.md",
        section: "Hello — `0x01` / `0x81`",
        statement: "Key agreement MUST NOT delay the controller's control loop, and the controller SHOULD compute at most one handshake at a time, serving connections in turn.",
    },
    Requirement {
        id: "P-076",
        document: "docs/PROTOCOL.md",
        section: "Goodbye — `0x0C` / `0x8C`",
        statement: "A connection row has two independent states: **allocated**, meaning a transport exists, and **bound**, meaning a session is running on it.",
    },
    Requirement {
        id: "P-077",
        document: "docs/PROTOCOL.md",
        section: "Goodbye — `0x0C` / `0x8C`",
        statement: "Sessions expire after 15 minutes without traffic, and the controller sends `CloseConnection` with reason `session_expired` so the row and the transport go at the same moment rather than leaving a socket the client believes is healthy.",
    },
    Requirement {
        id: "P-080",
        document: "docs/PROTOCOL.md",
        section: "Signed requests",
        statement: "The controller MUST take these steps in this order: 1. Open the sealed body. A `req_id` P-022 refuses is dropped unanswered, and a tag that fails is P-051's error 10. 2. For a `Command 0x08`, look up `(client_id, cmd_id)` in the dedup table, with the `client_id` the session is bound to. A match whose operation-hash differs is P-124's `rejected`; a match that agrees and was left **in flight** by a reset is answered from the state store, below; any other match is answered per P-120. None of them executes here. 3. For a `Command 0x08`, reserve a dedup entry marked **in flight** and persist it. A failure here is P-079's error 7, and a full table is P-122's. 4. Execute. 5. For a `Command 0x08`, mark the reserved entry **complete**, carrying the outcome it produced.",
    },
    Requirement {
        id: "P-079",
        document: "docs/PROTOCOL.md",
        section: "Signed requests",
        statement: "If persisting the dedup entry fails, the command MUST NOT execute.",
    },
    Requirement {
        id: "P-082",
        document: "docs/PROTOCOL.md",
        section: "Signed requests",
        statement: "P-022's `req_id` window refuses a replay and `cmd_id` suppresses a duplicate.",
    },
    Requirement {
        id: "P-083",
        document: "docs/PROTOCOL.md",
        section: "Signed requests",
        statement: "A write's inner body MUST NOT exceed `MAX_OPERATION` (960 bytes).",
    },
    Requirement {
        id: "P-250",
        document: "docs/PROTOCOL.md",
        section: "Clients, roles and invites",
        statement: "Every occupied slot MUST hold a **role** from [REGISTRY.md](protocol/REGISTRY.md): `owner`, `admin` or `viewer`.",
    },
    Requirement {
        id: "P-251",
        document: "docs/PROTOCOL.md",
        section: "Clients, roles and invites",
        statement: "A request a slot's role does not permit MUST be refused before it changes anything.",
    },
    Requirement {
        id: "P-252",
        document: "docs/PROTOCOL.md",
        section: "Invite — `0x15` / `0x95`",
        statement: "The controller MUST take these steps in this order, once P-080's first step has opened the request and before anything is stored: 1. the sender's role may propose this role (P-251), or outcome 2 `unauthorised`; 2. no occupied slot holds `invitee`, or outcome 4 `known_key`; 3. an admin sender has budget left (P-254), or outcome 6 `no_budget`; 4. a slot could be allocated to this role now (P-258), or outcome 5 `table_full`; 5. a pending-invite row this sender may take is free (P-253), or outcome 3 `invites_full`.",
    },
    Requirement {
        id: "P-253",
        document: "docs/PROTOCOL.md",
        section: "Invite — `0x15` / `0x95`",
        statement: "The controller MUST keep pending invites in a table of `MAX_INVITES` rows, in RAM, and MUST refuse rather than evict (P-003).",
    },
    Requirement {
        id: "P-254",
        document: "docs/PROTOCOL.md",
        section: "Invite — `0x15` / `0x95`",
        statement: "An `admin` slot MUST hold a **proposal budget** of `INVITE_BUDGET`, in a FRAM record of its own beside the slot's key record and never inside it (P-239), and each outcome 1 `proposed` MUST spend one.",
    },
    Requirement {
        id: "P-255",
        document: "docs/PROTOCOL.md",
        section: "Approve — `0x16` / `0x96`",
        statement: "The controller MUST take these steps in this order, once P-080's first step has opened the request: 1. a pending invite has this `nonce`, or outcome 4 `unknown_invite`; 2. the sender may make this decision, or outcome 3 `unauthorised`, and the invite stays pending. An owner may approve or decline; the invite's own inviter, by `client_id` and generation, may decline; 3. a decline withdraws the invite and is answered outcome 2 `declined`; 4. the inviter's slot still holds the inviter's generation and its role may still propose this role (P-251), or outcome 5 `inviter_gone`; 5. `reveal` opens the commitment and `proof` verifies (P-257), or outcome 6 `refused`; 6. no occupied slot holds the invitee's key, or outcome 7 `known_key`; 7. a slot can be allocated to this role (P-258), or outcome 8 `table_full`, and the invite stays pending; 8. the slot is written under P-239 with the invite's role, `client_kind`, `label` and key and the admission key P-257's check computed, or outcome 9 `not_stored`, the invite stays pending, and the controller MUST raise a class A `concern raised` (`0x0501`) at condition `client table write failed`.",
    },
    Requirement {
        id: "P-257",
        document: "docs/PROTOCOL.md",
        section: "Approve — `0x16` / `0x96`",
        statement: "An invite's cryptography is computed over this transcript, 126 bytes, each integer as P-040 fixes it: `transcript = device_id[16] | CS[32] | epoch:u32 | suite:u8 | role:u8 | inviter:u32 | inviter_generation:u32 | IS[32] | N_c[16] | N_i[16] commitment = SHA-256(\"km43/v1/invite-commit\" | IS | N_i) proof = HMAC(admit_key, \"km43/v1/invite-proof\" | transcript)[0..16] confirm = HMAC(admit_key, \"km43/v1/invite-confirm\" | transcript | client_id:u32 | generation:u32)[0..16] digits = u32be(SHA-256(\"km43/v1/invite-sas\" | transcript)[0..4]) mod 1000000` `admit_key` is P-238's for the invitee's key and the controller's, and `suite` is the inviter's slot's.",
    },
    Requirement {
        id: "P-256",
        document: "docs/PROTOCOL.md",
        section: "Remove — `0x17` / `0x97`",
        statement: "The controller MUST take these steps in this order, once P-080's first step has opened the request: 1. the sender's role may remove (P-251), or outcome 4 `unauthorised`; 2. the slot is occupied and holds this generation, or outcome 2 `gone`; 3. the slot is not `owner`, and is not `viewer` unless the sender is an owner, or outcome 3 `protected`; 4. every session bound to the slot, except the sender's own, is unbound; every invite the slot proposed is withdrawn (P-253); the slot is freed under P-239, which advances its generation; its budget is erased (P-254); 5. a class A `client removed` (`0x0605`) is raised and the request is answered outcome 1 `removed`. A sender that removed its own slot is unbound once the answer is sent.",
    },
    Requirement {
        id: "P-258",
        document: "docs/PROTOCOL.md",
        section: "Remove — `0x17` / `0x97`",
        statement: "A slot MUST be allocated to an `admin` only while fewer than `MAX_ADMINS` slots hold `admin`, and to a `viewer` only while fewer than `MAX_VIEWERS` hold `viewer`.",
    },
    Requirement {
        id: "P-093",
        document: "docs/PROTOCOL.md",
        section: "State and events",
        statement: "Every field carrying a **wall-clock time** MUST be omitted when the clock has never been set, and a zero MUST NOT be sent to mean *unknown*.",
    },
    Requirement {
        id: "P-168",
        document: "docs/PROTOCOL.md",
        section: "State and events",
        statement: "Acknowledging a concern moves `state` from **1 `active` to 2 `active_acked`** and never to 3, 4 or 5.",
    },
    Requirement {
        id: "P-180",
        document: "docs/PROTOCOL.md",
        section: "State and events",
        statement: "`5 cleared` is terminal, and a concern row leaves the table only carrying it.",
    },
    Requirement {
        id: "P-208",
        document: "docs/PROTOCOL.md",
        section: "State and events",
        statement: "A page ends at its **row cap** or its **byte cap**, whichever binds first, and a row is never split across two pages.",
    },
    Requirement {
        id: "P-145",
        document: "docs/PROTOCOL.md",
        section: "Inventory — `0x0D` / `0x8D`",
        statement: "An `Inventory 0x8D` whose `outcome` is not 1 MUST carry `next = 0`, MUST carry no rows, and MUST NOT carry key 7.",
    },
    Requirement {
        id: "P-146",
        document: "docs/PROTOCOL.md",
        section: "Inventory — `0x0D` / `0x8D`",
        statement: "The controller MUST NOT pin a walk.",
    },
    Requirement {
        id: "P-147",
        document: "docs/PROTOCOL.md",
        section: "Inventory — `0x0D` / `0x8D`",
        statement: "A request that was answered, in whole or in part, is **outcome 1 `ok`**.",
    },
    Requirement {
        id: "P-148",
        document: "docs/PROTOCOL.md",
        section: "Inventory — `0x0D` / `0x8D`",
        statement: "`topo_digest` is the **leftmost 8 bytes of SHA-256** over this preimage, and over nothing else: `\"km43/v1/topo-digest\" 19 bytes ASCII, no trailing NUL ‖ rev 4 bytes, big-endian ‖ for each descriptor row, in canonical order: the row's encoded CBOR map, byte for byte — exactly the bytes that appear inside the `rows` array of an Inventory 0x8D page canonical order = `what` ascending, then the row's own id ascending in a kind` An absent optional key contributes nothing, because it is not in the bytes.",
    },
    Requirement {
        id: "P-149",
        document: "docs/PROTOCOL.md",
        section: "Inventory — `0x0D` / `0x8D`",
        statement: "`rev` and `topo_digest` are **one identity**.",
    },
    Requirement {
        id: "P-200",
        document: "docs/PROTOCOL.md",
        section: "Inventory — `0x0D` / `0x8D`",
        statement: "`cmp = 0` is reserved in **every** message that names a component scope, and it means *the device as a whole*.",
    },
    Requirement {
        id: "P-201",
        document: "docs/PROTOCOL.md",
        section: "Inventory — `0x0D` / `0x8D`",
        statement: "An absent `cmds`, on a `ComponentRow` or a `DeviceRow`, means that scope accepts **no** commands.",
    },
    Requirement {
        id: "P-187",
        document: "docs/PROTOCOL.md",
        section: "Inventory — `0x0D` / `0x8D`",
        statement: "Neither `parent` chain — `DeviceRow` key 7 or `ComponentRow` key 3 — may form a cycle, and neither may exceed `MAX_TOPOLOGY_DEPTH`.",
    },
    Requirement {
        id: "P-202",
        document: "docs/PROTOCOL.md",
        section: "Inventory — `0x0D` / `0x8D`",
        statement: "`DeviceRow` key 3 `addr` MUST be present when the row's `bus` has an addressed `transport` — `rs485`, `can` or `ip` — and MUST be **unique within a `bus`**.",
    },
    Requirement {
        id: "P-203",
        document: "docs/PROTOCOL.md",
        section: "Inventory — `0x0D` / `0x8D`",
        statement: "A component whose `rollup` is set MUST have its values **already accounted for** in the named component's values, and a client MUST NOT combine the two under **any** aggregation — not a sum, not a maximum, not a mean.",
    },
    Requirement {
        id: "P-204",
        document: "docs/PROTOCOL.md",
        section: "Inventory — `0x0D` / `0x8D`",
        statement: "`unit` and `scale` MUST be **absent** from a `SignalRow` or a `ParamRow` whose `kind` is below `0xF000`, and MUST be **present** when that `kind` is in the vendor range `0xF000`–`0xFFFF`.",
    },
    Requirement {
        id: "P-205",
        document: "docs/PROTOCOL.md",
        section: "Inventory — `0x0D` / `0x8D`",
        statement: "`DeviceRow` key 12 `since` MUST be set to the current `rev` whenever the **physical instrument** behind that row changes, and `ComponentRow` key 9 `since` MUST be set to the current `rev` whenever **that row's meaning** changes.",
    },
    Requirement {
        id: "P-206",
        document: "docs/PROTOCOL.md",
        section: "Inventory — `0x0D` / `0x8D`",
        statement: "`ebase` is the **label of element 0**, not its position: element `k` of a `Series` carries the label `ebase` + `k`, and a `SignalRow` that omits `ebase` is read as `ebase = 1`.",
    },
    Requirement {
        id: "P-207",
        document: "docs/PROTOCOL.md",
        section: "Inventory — `0x0D` / `0x8D`",
        statement: "`(dev, cmp, kind, shape, vtype, domain, point, dir)` MUST be **unique within a `rev`** across all `SignalRow`s, and `(dev, cmp, kind, vtype, domain, point, dir)` across all `ParamRow`s, which carry no `shape`.",
    },
    Requirement {
        id: "P-198",
        document: "docs/PROTOCOL.md",
        section: "Readings — `0x0E` / `0x8E`",
        statement: "The resolved selection of a `ReadSignals` is the **set union** of its selectors, deduplicated by `sig`, ordered by **`sig` ascending**.",
    },
    Requirement {
        id: "P-199",
        document: "docs/PROTOCOL.md",
        section: "Readings — `0x0E` / `0x8E`",
        statement: "A responder MUST evaluate a response's outcomes in **ascending order** and return the first that applies.",
    },
    Requirement {
        id: "P-185",
        document: "docs/PROTOCOL.md",
        section: "Readings — `0x0E` / `0x8E`",
        statement: "Every value position on this wire is a signed integer in the `i32` range: `Sample` key 2, `Series` key 3, and every value position of a descriptor row.",
    },
    Requirement {
        id: "P-196",
        document: "docs/PROTOCOL.md",
        section: "Readings — `0x0E` / `0x8E`",
        statement: "A reading's value key MUST be present exactly when its `q` byte's validity is `1 ok` or `2 stale`, and absent otherwise.",
    },
    Requirement {
        id: "P-164",
        document: "docs/PROTOCOL.md",
        section: "Readings — `0x0E` / `0x8E`",
        statement: "A source reporting an operating state this controller has no normalized value for MUST be published at **`validity 8 unnamed_state` with no value**, and MUST raise a `Concern` carrying the source's own code in `raw` and its namespace in `vns`.",
    },
    Requirement {
        id: "P-125",
        document: "docs/PROTOCOL.md",
        section: "Readings — `0x0E` / `0x8E`",
        statement: "A **receiver** meeting a value drawn from an enum space it does not recognise MUST surface **that one reading** as unrecognised: never mapped onto a member it does know, never rendered as a bare number, and never replaced with a fallback.",
    },
    Requirement {
        id: "P-197",
        document: "docs/PROTOCOL.md",
        section: "Readings — `0x0E` / `0x8E`",
        statement: "A series reading's key 2 MUST be exactly `n` bytes, where `n` is the signal's declared length, and byte `k` is element `k`'s `q`.",
    },
    Requirement {
        id: "P-209",
        document: "docs/PROTOCOL.md",
        section: "Concerns — `0x0F` / `0x8F`",
        statement: "A `ReadConcerns` walk is ordered by **`cid` ascending**; `from` and `next` are `cid` ids and never ordinals, and a page is a prefix of that order.",
    },
    Requirement {
        id: "P-210",
        document: "docs/PROTOCOL.md",
        section: "Concerns — `0x0F` / `0x8F`",
        statement: "`Concerns` key 2 `seq` is the pin a walk is held against.",
    },
    Requirement {
        id: "P-211",
        document: "docs/PROTOCOL.md",
        section: "Concerns — `0x0F` / `0x8F`",
        statement: "`Concern` key 9 `age` is a duration in **seconds** on P-004's tick and is always present.",
    },
    Requirement {
        id: "P-094",
        document: "docs/PROTOCOL.md",
        section: "Subscribe — `0x03` / `0x83`",
        statement: "The transition from replay to live MUST be atomic: the controller replays retained events from `accepted_from_seq` and continues into live delivery with no window between.",
    },
    Requirement {
        id: "P-104",
        document: "docs/PROTOCOL.md",
        section: "Subscribe — `0x03` / `0x83`",
        statement: "`accepted_from_seq` MUST be `current_seq + 1` when `from_seq` is 0, and `max(from_seq, oldest_seq)` otherwise, where `current_seq` is the newest `seq` the log holds at the moment the `Subscribe` is answered.",
    },
    Requirement {
        id: "P-144",
        document: "docs/PROTOCOL.md",
        section: "Subscribe — `0x03` / `0x83`",
        statement: "**`seq` 0 is not a position.** The first record a log ever writes is `seq = 1`, and 0 in `oldest_seq` or `current_seq` means *this log holds no record*.",
    },
    Requirement {
        id: "P-095",
        document: "docs/PROTOCOL.md",
        section: "Subscribe — `0x03` / `0x83`",
        statement: "`gap` MUST be true when `from_seq` is not 0 and `from_seq < oldest_seq`.",
    },
    Requirement {
        id: "P-212",
        document: "docs/PROTOCOL.md",
        section: "Event — `0x04`",
        statement: "`0x0102` and `0x0902` each carry an **array**, and a controller MUST NOT send one that is empty.",
    },
    Requirement {
        id: "P-213",
        document: "docs/PROTOCOL.md",
        section: "Event — `0x04`",
        statement: "A `0x0901` MUST carry the reason the revision moved.",
    },
    Requirement {
        id: "P-214",
        document: "docs/PROTOCOL.md",
        section: "Event — `0x04`",
        statement: "A boot body MUST carry keys 5 and 6 together or not at all and only with reason 2, and keys 7 and 8 together with reason 5 and no other; a receiver MUST refuse one that breaks either.",
    },
    Requirement {
        id: "P-215",
        document: "docs/PROTOCOL.md",
        section: "Event — `0x04`",
        statement: "These bodies MUST carry every listed key except `Time set.old`, which MUST be omitted when the previous clock was unknown (P-093).",
    },
    Requirement {
        id: "P-182",
        document: "docs/PROTOCOL.md",
        section: "Event — `0x04`",
        statement: "The controller MUST NOT enqueue more than one `0x0102` and one `0x0902` per tick.",
    },
    Requirement {
        id: "P-096",
        document: "docs/PROTOCOL.md",
        section: "Event — `0x04`",
        statement: "`seq` is strictly increasing but **NOT contiguous**.",
    },
    Requirement {
        id: "P-097",
        document: "docs/PROTOCOL.md",
        section: "Event — `0x04`",
        statement: "A client MUST NOT re-read the log on every hole.",
    },
    Requirement {
        id: "P-119",
        document: "docs/PROTOCOL.md",
        section: "Event — `0x04`",
        statement: "A subscribed client MUST compare the highest `Event` `seq` it has accepted against the newest `seq` reported inside the sealed responses it is already receiving — `Readings 0x8E` key 1, `SubscribeAck 0x83` key 3 `current_seq`, and `HelloReport` key 8 `log_newest_seq` — and MUST treat a divergence that persists across a bounded number of such responses as a broken stream: surface it, reconnect, and reconcile with a single bounded `ReadLog`, exactly as P-097 permits for an unexplained hole.",
    },
    Requirement {
        id: "P-098",
        document: "docs/PROTOCOL.md",
        section: "Event — `0x04`",
        statement: "The controller MUST send one separately sealed copy of an event to each subscribed session.",
    },
    Requirement {
        id: "P-099",
        document: "docs/PROTOCOL.md",
        section: "ReadLog — `0x05` / `0x85`",
        statement: "If `from_seq < oldest_seq` the controller MUST answer from `oldest_seq`.",
    },
    Requirement {
        id: "P-100",
        document: "docs/PROTOCOL.md",
        section: "Configuration",
        statement: "`expected_version` MUST be checked and a mismatch refused with `stale_version`.",
    },
    Requirement {
        id: "P-108",
        document: "docs/PROTOCOL.md",
        section: "Configuration",
        statement: "A section that has never been written or that the controller holds but cannot read MUST be answered with `version` 0 and no key 3, and a readable written section MUST carry a nonzero `version` and key 3, so a client MUST refuse a `Config` in which the two disagree.",
    },
    Requirement {
        id: "P-101",
        document: "docs/PROTOCOL.md",
        section: "Configuration",
        statement: "Validation happens on the controller and rejection is loud.",
    },
    Requirement {
        id: "P-102",
        document: "docs/PROTOCOL.md",
        section: "Configuration",
        statement: "Writes MUST land in the inactive **A/B slot** with a sequence number and CRC and take effect by an atomic pointer flip.",
    },
    Requirement {
        id: "P-103",
        document: "docs/PROTOCOL.md",
        section: "Configuration",
        statement: "Every behaviour section MUST carry a `shadow` flag readable by a client, under one key number that is the same in all four behaviour sections.",
    },
    Requirement {
        id: "P-106",
        document: "docs/PROTOCOL.md",
        section: "Section bodies",
        statement: "A field marked `secret` MUST NOT be returned in a `Config` body, and a client MUST refuse a `Config` body that carries one.",
    },
    Requirement {
        id: "P-107",
        document: "docs/PROTOCOL.md",
        section: "Section bodies",
        statement: "A `SetConfig` of the network section that carries an `ssid` and no `psk` MUST keep the passphrase the controller already holds only when that `ssid` is byte-for-byte the one it is held for, and MUST be refused with outcome 3 `invalid` otherwise, including when no passphrase is held at all.",
    },
    Requirement {
        id: "P-216",
        document: "docs/PROTOCOL.md",
        section: "Wi-Fi",
        statement: "A controller MUST set capability bit 8 in `Hello 0x81` exactly when it answers both `WifiScan` and `WifiStatus`, and a client MUST NOT send either to a controller that does not set it.",
    },
    Requirement {
        id: "P-217",
        document: "docs/PROTOCOL.md",
        section: "Wi-Fi",
        statement: "The controller MUST hold at most one list, the one from the most recent scan that completed, and MUST answer every `WifiScan` from it.",
    },
    Requirement {
        id: "P-218",
        document: "docs/PROTOCOL.md",
        section: "Wi-Fi",
        statement: "A `WifiScan` with `refresh` true that meets a running scan joins it and carries no key 2.",
    },
    Requirement {
        id: "P-219",
        document: "docs/PROTOCOL.md",
        section: "Wi-Fi",
        statement: "`WifiStatus 0x92` MUST carry key 1, and MUST carry keys 2 and 3 exactly when the controller holds a report from the comms processor's current boot.",
    },
    Requirement {
        id: "P-220",
        document: "docs/PROTOCOL.md",
        section: "Wi-Fi",
        statement: "The controller MUST write a `0x0806` record when the version, state and reason it holds differ from those it last announced, and MUST NOT write one for `2 joining`.",
    },
    Requirement {
        id: "P-221",
        document: "docs/PROTOCOL.md",
        section: "Wi-Fi",
        statement: "A client MUST present the list and the status as the comms processor's report, and no decision on the controller or a client that grants anything MAY rest on them.",
    },
    Requirement {
        id: "P-110",
        document: "docs/PROTOCOL.md",
        section: "Time",
        statement: "Setting the clock is a **signed write**.",
    },
    Requirement {
        id: "P-111",
        document: "docs/PROTOCOL.md",
        section: "Time",
        statement: "The controller MUST record `source` in the resulting `time set` event (`0x0604`).",
    },
    Requirement {
        id: "P-112",
        document: "docs/PROTOCOL.md",
        section: "Time",
        statement: "Events already written with no timestamp MUST NOT be retroactively stamped.",
    },
    Requirement {
        id: "P-113",
        document: "docs/PROTOCOL.md",
        section: "Time",
        statement: "A `Time 0x0A` whose `at` falls outside the **plausibility window** MUST be refused with `TimeAck` outcome 2 `rejected`, and the clock MUST NOT move.",
    },
    Requirement {
        id: "P-114",
        document: "docs/PROTOCOL.md",
        section: "Time",
        statement: "A `Time 0x0A` whose `at` is earlier than the timestamp of the newest log record carrying one MUST be refused with `TimeAck` outcome 4 `needs_button`.",
    },
    Requirement {
        id: "P-115",
        document: "docs/PROTOCOL.md",
        section: "Time",
        statement: "When the clock was **already known** and an accepted set moves it by more than `TIME_STEP_ALARM`, **one hour**, in either direction, the controller MUST raise a class A `concern raised` (`0x0501`) at condition `clock stepped`, alongside the `time set` record P-111 requires, whichever message moved it.",
    },
    Requirement {
        id: "P-116",
        document: "docs/PROTOCOL.md",
        section: "Time",
        statement: "A `Time 0x0A` that the floor would refuse MUST be accepted **while a floor override is armed**, and the controller MUST raise a class A `concern raised` (`0x0501`) at condition `floor overridden`, alongside the `time set` record P-111 requires.",
    },
    Requirement {
        id: "P-117",
        document: "docs/PROTOCOL.md",
        section: "Time",
        statement: "The floor override: 1. Is armed **only** by a press pattern at the panel distinguishable from P-066's enrolment gesture and from the factory-reset hold. An enrolment gesture MUST NOT arm an override; P-066's first-enrolment power-on window MUST NOT arm one either. 2. Is **single-use**: it authorises exactly one accepted floor-crossing `Time 0x0A`, and MUST be cleared on use, on release of the button, and on a bounded timeout no longer than P-066's 120 seconds. 3. Is evaluated at the **instant the controller processes the operation**. It is not a window a client can be told about in advance and it is not P-066's 120-second window. 4. Does not lift P-118's rate limit. A `Time 0x0A` refused for the rate limit is refused with an override armed exactly as without one.",
    },
    Requirement {
        id: "P-118",
        document: "docs/PROTOCOL.md",
        section: "Time",
        statement: "The controller MUST accept at most one `Time 0x0A` per **15 minutes** on P-004's tick, matching the `TimeOffer` bound in [LINK.md](protocol/LINK.md) L-151.",
    },
    Requirement {
        id: "P-120",
        document: "docs/PROTOCOL.md",
        section: "Commands",
        statement: "The dedup table MUST be keyed `(client_id, cmd_id, operation-hash)`, where **operation-hash is the leftmost 8 bytes of SHA-256 over the operation body exactly as it opened** (P-048, and never over a re-encoding).",
    },
    Requirement {
        id: "P-121",
        document: "docs/PROTOCOL.md",
        section: "Commands",
        statement: "The dedup table MUST live in **FRAM** and survive a reset.",
    },
    Requirement {
        id: "P-122",
        document: "docs/PROTOCOL.md",
        section: "Commands",
        statement: "The table holds `MAX_CMD_DEDUP` entries for 10 minutes.",
    },
    Requirement {
        id: "P-124",
        document: "docs/PROTOCOL.md",
        section: "Commands",
        statement: "A `Command` whose `(client_id, cmd_id)` matches a live dedup entry but whose operation-hash does not MUST be answered `Ack 0x88` outcome 2 `rejected`, with `detail` saying the `cmd_id` was reused for a different command.",
    },
    Requirement {
        id: "P-123",
        document: "docs/PROTOCOL.md",
        section: "Commands",
        statement: "`inhibited` and `shadowed` are outcomes, not errors.",
    },
    Requirement {
        id: "P-130",
        document: "docs/PROTOCOL.md",
        section: "Firmware",
        statement: "The comms processor delivers the controller image; the **controller's own bootloader** verifies the signature.",
    },
    Requirement {
        id: "P-131",
        document: "docs/PROTOCOL.md",
        section: "Firmware",
        statement: "A comms image is verified twice: the controller authorises the release, and the **comms processor's own secure boot** verifies the image signature before executing it.",
    },
    Requirement {
        id: "P-140",
        document: "docs/PROTOCOL.md",
        section: "Errors",
        statement: "See P-055.",
    },
    Requirement {
        id: "P-141",
        document: "docs/PROTOCOL.md",
        section: "Errors",
        statement: "A request that reaches its handler MUST be answered by its own response type, carrying an outcome.",
    },
    Requirement {
        id: "P-142",
        document: "docs/PROTOCOL.md",
        section: "Errors",
        statement: "Which of the two shapes an `Error` takes is decided by whether the **sender** holds a session for that `session_id` — sealed under the session's keys when it does, bare when it does not.",
    },
    Requirement {
        id: "P-143",
        document: "docs/PROTOCOL.md",
        section: "Errors",
        statement: "A request whose type requires a session, arriving before any `Hello` has succeeded on that connection, MUST be refused with error 4.",
    },
    Requirement {
        id: "L-001",
        document: "docs/protocol/LINK.md",
        section: "Reserved range",
        statement: "A receiver MUST refuse a link-local message arriving from the side the direction column does not permit, with code 256 — the same code as an opcode it does not know.",
    },
    Requirement {
        id: "L-002",
        document: "docs/protocol/LINK.md",
        section: "A link-local opcode on a client transport is a protocol error",
        statement: "The comms processor MUST NOT emit a frame in this range on BLE, WebSocket, MQTT or USB, and MUST NOT accept one from them.",
    },
    Requirement {
        id: "L-003",
        document: "docs/protocol/LINK.md",
        section: "A link-local opcode on a client transport is a protocol error",
        statement: "In the other direction: the comms processor MUST drop a frame the controller emits with a link-local `type` and a non-zero `session_id`, and MUST answer code 263 rather than routing it to a client.",
    },
    Requirement {
        id: "L-010",
        document: "docs/protocol/LINK.md",
        section: "Envelope, framing and correlation",
        statement: "A link-local frame MUST use the client protocol's envelope, framing and size limits unchanged: CBOR with integer keys (P-011), definite lengths, unknown keys skipped (P-013), unknown enum discriminants rejected (P-014), every listed key required unless it is marked *optional* (P-015), RFC 8949 §4.2 deterministic encoding (P-016), and the 1024-byte payload cap.",
    },
    Requirement {
        id: "L-011",
        document: "docs/protocol/LINK.md",
        section: "Envelope, framing and correlation",
        statement: "Every duration in this document MUST be measured on P-004's monotonic tick since boot, never on the wall clock.",
    },
    Requirement {
        id: "L-012",
        document: "docs/protocol/LINK.md",
        section: "Envelope, framing and correlation",
        statement: "Every link-local frame MUST carry `session_id = 0`.",
    },
    Requirement {
        id: "L-013",
        document: "docs/protocol/LINK.md",
        section: "Envelope, framing and correlation",
        statement: "The sender MUST allocate `req_id` from its own counter, independent of the peer's, and both counters wrap.",
    },
    Requirement {
        id: "L-014",
        document: "docs/protocol/LINK.md",
        section: "Envelope, framing and correlation",
        statement: "A side MUST NOT have more than 4 link-local requests outstanding, and a receiver MUST answer a peer's fifth with code 262.",
    },
    Requirement {
        id: "L-015",
        document: "docs/protocol/LINK.md",
        section: "Envelope, framing and correlation",
        statement: "A sender MUST treat a request unanswered after 500 ms as failed and MUST retry it with the same `req_id`, up to 3 attempts; after the third it MUST give the request up and report it failed to whatever asked.",
    },
    Requirement {
        id: "L-020",
        document: "docs/protocol/LINK.md",
        section: "Unauthenticated by design, and where that stops",
        statement: "**While the two chips are on one board**, a link-local message MUST NOT carry a MAC, and the two chips MUST NOT share a key for this purpose.",
    },
    Requirement {
        id: "L-021",
        document: "docs/protocol/LINK.md",
        section: "Unauthenticated by design, and where that stops",
        statement: "Power authority runs one way only.",
    },
    Requirement {
        id: "L-022",
        document: "docs/protocol/LINK.md",
        section: "Unauthenticated by design, and where that stops",
        statement: "The controller MUST count the sessions it sheds under backpressure, per hour, and on the third inside an hour it MUST enter the ladder below at its first rung: every connection and session dropped, comms link lost (`0x0801`) logged, control unaffected.",
    },
    Requirement {
        id: "L-023",
        document: "docs/protocol/LINK.md",
        section: "Unauthenticated by design, and where that stops",
        statement: "The first session shed for backpressure inside an hour MUST be logged as class A **sessions shed for backpressure** (`0x0804`), allocated in [REGISTRY.md](REGISTRY.md), and not as `0x0801`.",
    },
    Requirement {
        id: "L-024",
        document: "docs/protocol/LINK.md",
        section: "Unauthenticated by design, and where that stops",
        statement: "If this link ever leaves the board, every link-local message MUST be authenticated under a link key held in the controller's storage and provisioned at manufacture.",
    },
    Requirement {
        id: "L-030",
        document: "docs/protocol/LINK.md",
        section: "LinkUp",
        statement: "Both sides MUST send `LinkUp` at boot, and either side MAY send it again at any time.",
    },
    Requirement {
        id: "L-031",
        document: "docs/protocol/LINK.md",
        section: "LinkUp",
        statement: "The controller MUST store field 4 of the last successful `LinkUp` and MUST report it as `fw_comms` in the client `Hello` response.",
    },
    Requirement {
        id: "L-032",
        document: "docs/protocol/LINK.md",
        section: "LinkUp",
        statement: "A client MAY display `fw_comms`, MUST NOT read it as confirmation that a comms image is installed, and no authorisation on either side MAY rest on it.",
    },
    Requirement {
        id: "L-033",
        document: "docs/protocol/LINK.md",
        section: "LinkUp",
        statement: "A side is linked once its own `LinkUp` has been answered: the answer carries the peer's statement, and answering proves the peer holds this side's.",
    },
    Requirement {
        id: "L-034",
        document: "docs/protocol/LINK.md",
        section: "LinkUp",
        statement: "`fw` in `LinkUp`, and `version` in `CommsRelease` and `CommsReleaseAck`, MUST be a semantic version whose build metadata names the commit it was built from: `MAJOR.MINOR.PATCH[-PRE]+gXXXXXXXX`, with the first eight lowercase hex digits of the commit id after the `g`.",
    },
    Requirement {
        id: "L-035",
        document: "docs/protocol/LINK.md",
        section: "LinkUp",
        statement: "The controller MUST send field 8 in every `LinkUp`, carrying the same 16 bytes `Discover 0x80` key 3 carries, and the comms processor MUST NOT send it.",
    },
    Requirement {
        id: "L-040",
        document: "docs/protocol/LINK.md",
        section: "boot_id is what makes a reboot visible",
        statement: "Each side MUST redraw `boot_id` randomly on every boot.",
    },
    Requirement {
        id: "L-041",
        document: "docs/protocol/LINK.md",
        section: "boot_id is what makes a reboot visible",
        statement: "On a `LinkUp` whose comms `boot_id` differs from the one last seen, the controller MUST immediately drop every connection that comms processor announced and every session bound to them.",
    },
    Requirement {
        id: "L-042",
        document: "docs/protocol/LINK.md",
        section: "boot_id is what makes a reboot visible",
        statement: "On a `LinkUp` whose controller `boot_id` differs from the one last seen, the comms processor MUST close every live client connection.",
    },
    Requirement {
        id: "L-050",
        document: "docs/protocol/LINK.md",
        section: "Version mismatch does not take the link down",
        statement: "On a link protocol major mismatch the controller MUST accept only `LinkUp`, `Heartbeat` and a `CommsReleaseAck` `0xE7` answering a release it sent itself; the comms processor MUST accept only `LinkUp`, `Heartbeat` and `CommsRelease`; everything else MUST be refused with code 261; a `CommsRelease` *request* arriving at the controller MUST be refused with code 256 exactly as it always is; and client frames MUST NOT be forwarded in either direction.",
    },
    Requirement {
        id: "L-051",
        document: "docs/protocol/LINK.md",
        section: "Version mismatch does not take the link down",
        statement: "On a minor version mismatch both sides MUST proceed at the lower of the two minor versions, exactly as with a client.",
    },
    Requirement {
        id: "L-060",
        document: "docs/protocol/LINK.md",
        section: "Who allocates what",
        statement: "The comms processor MUST allocate every connection handle itself, from an incrementing counter in the range 1 to 0xFFFF, skipping handles currently in use, and MUST NOT ever allocate 0.",
    },
    Requirement {
        id: "L-061",
        document: "docs/protocol/LINK.md",
        section: "Who allocates what",
        statement: "When all eight connection rows are allocated the controller MUST answer `ClientConnected` with `refused_table_full`, and the comms processor MUST then close that transport with a reason a client can show.",
    },
    Requirement {
        id: "L-062",
        document: "docs/protocol/LINK.md",
        section: "Who allocates what",
        statement: "The controller MUST answer a frame carrying a connection handle it was never told about with code 259, and MUST drop the frame.",
    },
    Requirement {
        id: "L-070",
        document: "docs/protocol/LINK.md",
        section: "ClientConnected",
        statement: "On answering `ClientConnected` with `accepted` the controller MUST attempt to mint a fresh challenge for that connection and hold it against the handle if successful.",
    },
    Requirement {
        id: "L-071",
        document: "docs/protocol/LINK.md",
        section: "ClientConnected",
        statement: "`ClientConnectedAck` MUST NOT carry the challenge.",
    },
    Requirement {
        id: "L-072",
        document: "docs/protocol/LINK.md",
        section: "ClientConnected",
        statement: "`peer` and `transport` are both assertions from the untrusted peer.",
    },
    Requirement {
        id: "L-080",
        document: "docs/protocol/LINK.md",
        section: "ClientDisconnected",
        statement: "The comms processor MUST NOT reuse a handle until the controller has answered its `ClientDisconnected` with `ClientDisconnectedAck`.",
    },
    Requirement {
        id: "L-090",
        document: "docs/protocol/LINK.md",
        section: "CloseConnection",
        statement: "The comms processor MUST treat `CloseConnection` with `conn = 0` as every connection, and MUST report in the ack's `closed` how many it actually closed.",
    },
    Requirement {
        id: "L-100",
        document: "docs/protocol/LINK.md",
        section: "Heartbeat",
        statement: "Each side MUST send a `Heartbeat` every 2 seconds and MUST answer the peer's immediately rather than on its own next tick.",
    },
    Requirement {
        id: "L-101",
        document: "docs/protocol/LINK.md",
        section: "Heartbeat",
        statement: "`conns` MUST count allocated connection rows, bound or not, and MUST NOT count bound sessions.",
    },
    Requirement {
        id: "L-102",
        document: "docs/protocol/LINK.md",
        section: "Heartbeat",
        statement: "When the two counts differ for three consecutive heartbeats the comms processor's count MUST be taken as the correct one, the controller MUST send `CloseConnection(conn = 0, reason = resync)`, and the comms processor MUST re-announce every live connection.",
    },
    Requirement {
        id: "L-110",
        document: "docs/protocol/LINK.md",
        section: "When the controller stops hearing the comms processor",
        statement: "Six seconds after the comms processor last answered one of the controller's requests the controller MUST treat the link as down, drop every connection and every session bound to one, and log comms link lost (`0x0801`).",
    },
    Requirement {
        id: "L-111",
        document: "docs/protocol/LINK.md",
        section: "When the controller stops hearing the comms processor",
        statement: "Sixty seconds after the comms processor last answered one of the controller's requests, or after the rail last came up if it has not answered since, the controller MUST cut the ESP32 power rail for 5 seconds, restore it, and log comms power cycled (`0x0802`) carrying the count.",
    },
    Requirement {
        id: "L-112",
        document: "docs/protocol/LINK.md",
        section: "When the controller stops hearing the comms processor",
        statement: "After 3 power cycles inside an hour the controller MUST stop cycling the rail for 15 minutes, MUST raise comms unrecoverable (`0x0803`), and MUST leave the rail off for those 15 minutes, unless its board cannot switch the rail back on after that long.",
    },
    Requirement {
        id: "L-113",
        document: "docs/protocol/LINK.md",
        section: "When the controller stops hearing the comms processor",
        statement: "While a comms firmware install is in flight the controller MUST suspend the ladder, and MUST resume it only when the install finishes or its 10-minute window lapses.",
    },
    Requirement {
        id: "L-114",
        document: "docs/protocol/LINK.md",
        section: "When the controller stops hearing the comms processor",
        statement: "The rail's declared fail state MUST be on, so that a controller reset is not also a comms reset.",
    },
    Requirement {
        id: "L-120",
        document: "docs/protocol/LINK.md",
        section: "When the comms processor stops hearing the controller",
        statement: "Six seconds after the controller last answered one of the comms processor's requests, and from its boot until the controller first answers, the comms processor MUST close every client connection, stop advertising over BLE, refuse new connections, and retry `LinkUp` every 2 seconds until the controller answers.",
    },
    Requirement {
        id: "L-121",
        document: "docs/protocol/LINK.md",
        section: "When the comms processor stops hearing the controller",
        statement: "A comms processor that has lost the controller MUST NOT serve a cached snapshot, MUST NOT answer a `Discover` from memory, and MUST NOT hold a client connection open while it waits.",
    },
    Requirement {
        id: "L-130",
        document: "docs/protocol/LINK.md",
        section: "Wi-Fi credentials",
        statement: "The controller MUST hold the master copy of the Wi-Fi credentials, and the comms processor MUST hold a cache of them in its own NVS.",
    },
    Requirement {
        id: "L-131",
        document: "docs/protocol/LINK.md",
        section: "Wi-Fi credentials",
        statement: "A `NetConfig` with `op = clear` MUST omit both `psk` and `ssid`, and a `psk` sent with `op = set` MUST be 8 to 63 bytes.",
    },
    Requirement {
        id: "L-132",
        document: "docs/protocol/LINK.md",
        section: "Wi-Fi credentials",
        statement: "`NetConfigAck` and `LinkUp.net_version` MUST report the version persisted in NVS, including a stored clear.",
    },
    Requirement {
        id: "L-133",
        document: "docs/protocol/LINK.md",
        section: "Wi-Fi credentials",
        statement: "The controller MUST push `NetConfig` after every `LinkUp` whose `net_version` does not equal its own version, and MUST NOT withhold the push because the version reported is higher.",
    },
    Requirement {
        id: "L-134",
        document: "docs/protocol/LINK.md",
        section: "Wi-Fi credentials",
        statement: "A nonzero-version `NetConfig` MUST carry `country` as exactly two bytes of ISO 3166-1 alpha-2.",
    },
    Requirement {
        id: "L-135",
        document: "docs/protocol/LINK.md",
        section: "Wi-Fi credentials",
        statement: "On a factory reset of a written network section, the controller MUST send `NetConfig` with `op = clear` at the incremented, nonzero section version, retaining the section's country and hostname, so a passphrase does not survive on a board that is about to be pulled and shipped somewhere.",
    },
    Requirement {
        id: "L-136",
        document: "docs/protocol/LINK.md",
        section: "Wi-Fi credentials",
        statement: "The comms processor MUST cache at most one network, and a `NetConfig` with `op = set` MUST replace what it holds rather than adding to it.",
    },
    Requirement {
        id: "L-137",
        document: "docs/protocol/LINK.md",
        section: "Wi-Fi credentials",
        statement: "A comms processor whose NVS write fails MUST answer `nvs_write_failed` and MUST keep running on the credentials it was given in RAM, and the controller MUST push again at the next `LinkUp` and log the failure.",
    },
    Requirement {
        id: "L-200",
        document: "docs/protocol/LINK.md",
        section: "Wi-Fi scan and join state",
        statement: "The controller MUST have at most one `WifiScan` outstanding, and MUST number them from 1 within each boot, never 0.",
    },
    Requirement {
        id: "L-201",
        document: "docs/protocol/LINK.md",
        section: "Wi-Fi scan and join state",
        statement: "For each `WifiScan` answered `started`, the comms processor MUST send exactly one `WifiScanResult` carrying its number, retried under L-015.",
    },
    Requirement {
        id: "L-202",
        document: "docs/protocol/LINK.md",
        section: "Wi-Fi scan and join state",
        statement: "The list MUST hold one row per SSID, for the strongest access point heard with it, ordered strongest first, at most `MAX_SCAN_APS`.",
    },
    Requirement {
        id: "L-203",
        document: "docs/protocol/LINK.md",
        section: "Wi-Fi scan and join state",
        statement: "The controller MUST treat a scan as failed when its `WifiScanAck` is not `started`, when the request is given up under L-015, when no result arrives within `SCAN_TIMEOUT_MS` (15 000 ms) of the `started`, and when the link goes down or the comms `boot_id` changes first.",
    },
    Requirement {
        id: "L-204",
        document: "docs/protocol/LINK.md",
        section: "Wi-Fi scan and join state",
        statement: "The comms processor MUST send `WifiState` once its own `LinkUp` has been answered (L-033) and again whenever what it would report changes.",
    },
    Requirement {
        id: "L-205",
        document: "docs/protocol/LINK.md",
        section: "Wi-Fi scan and join state",
        statement: "`version` MUST be the version of the credentials the radio is using.",
    },
    Requirement {
        id: "L-206",
        document: "docs/protocol/LINK.md",
        section: "Wi-Fi scan and join state",
        statement: "The comms processor MUST NOT have more than one `WifiState` outstanding.",
    },
    Requirement {
        id: "L-207",
        document: "docs/protocol/LINK.md",
        section: "Wi-Fi scan and join state",
        statement: "The controller MUST NOT act on a `WifiScanResult` or a `WifiState` beyond holding, answering and recording it.",
    },
    Requirement {
        id: "L-140",
        document: "docs/protocol/LINK.md",
        section: "Time offers",
        statement: "The controller MUST refuse the first set after boot with `refused_implausible` unless `unix_ms` falls between the **monotonic floor** — the timestamp of the newest log record it holds — and ten years after that floor.",
    },
    Requirement {
        id: "L-141",
        document: "docs/protocol/LINK.md",
        section: "Time offers",
        statement: "The controller MUST NOT apply the floor to an offer that moves an already-known clock inside the 5-second cap, including one that moves it backwards.",
    },
    Requirement {
        id: "L-142",
        document: "docs/protocol/LINK.md",
        section: "Time offers",
        statement: "A controller that holds no log record carrying a timestamp MUST use the firmware build timestamp as the floor, and MUST NOT use it as the floor in any other case.",
    },
    Requirement {
        id: "L-143",
        document: "docs/protocol/LINK.md",
        section: "Time offers",
        statement: "A boot at which the RTC reports its backup domain invalid MUST be recorded in the class A boot record (`0x0601`).",
    },
    Requirement {
        id: "L-150",
        document: "docs/protocol/LINK.md",
        section: "Time offers",
        statement: "Once the clock is known the controller MUST refuse an offer that would move it by more than 5 seconds, in either direction, with `refused_step_too_large`.",
    },
    Requirement {
        id: "L-151",
        document: "docs/protocol/LINK.md",
        section: "Time offers",
        statement: "The controller MUST accept at most one offer per 15 minutes, measured on P-004's monotonic tick, and MUST refuse and count every offer arriving inside that window with `refused_rate_limited`.",
    },
    Requirement {
        id: "L-152",
        document: "docs/protocol/LINK.md",
        section: "Time offers",
        statement: "The controller MUST NOT emit `TimeOfferAck` outcome 3.",
    },
    Requirement {
        id: "L-160",
        document: "docs/protocol/LINK.md",
        section: "Time offers",
        statement: "The controller MUST record any accepted clock change of more than 5 seconds, whatever moved it, in the `time set` event (`0x0604`), carrying the old value alongside the new one and the source that set it.",
    },
    Requirement {
        id: "L-161",
        document: "docs/protocol/LINK.md",
        section: "Time offers",
        statement: "A behaviour whose decision actuates MUST NOT treat time the controller never had as time that has passed, and a clock change MUST NOT cause a run the new clock says was missed.",
    },
    Requirement {
        id: "L-162",
        document: "docs/protocol/LINK.md",
        section: "Time offers",
        statement: "The link-local `source` value MUST NOT be copied into the `0x0604` record; the record MUST name the clock as having been set by an offer through the comms processor, distinctly from a client `Time` operation.",
    },
    Requirement {
        id: "L-169",
        document: "docs/protocol/LINK.md",
        section: "Comms firmware release",
        statement: "Policy MUST NOT permit an arbitrary downgrade.",
    },
    Requirement {
        id: "L-170",
        document: "docs/protocol/LINK.md",
        section: "Comms firmware release",
        statement: "The controller MUST send `authorise` carrying the version, the image length and the digest, and MUST record the authorised digest in FRAM.",
    },
    Requirement {
        id: "L-171",
        document: "docs/protocol/LINK.md",
        section: "Comms firmware release",
        statement: "The comms processor MUST hash the image it received, MUST answer `refused_digest_mismatch` and install nothing when that hash does not equal the authorised digest, and MUST refuse with code 264 an image that matches no authorisation at all.",
    },
    Requirement {
        id: "L-172",
        document: "docs/protocol/LINK.md",
        section: "Comms firmware release",
        statement: "The comms processor MUST write the image to the inactive partition, and its own secure boot MUST verify the image signature before executing it.",
    },
    Requirement {
        id: "L-173",
        document: "docs/protocol/LINK.md",
        section: "Comms firmware release",
        statement: "`activate` MUST reboot the comms processor onto the new image, the slot MUST be marked good only on `confirm_healthy` sent after the link comes back on the new version, and without that confirmation the comms processor's own rollback MUST put the old image back.",
    },
    Requirement {
        id: "L-174",
        document: "docs/protocol/LINK.md",
        section: "Comms firmware release",
        statement: "The controller MUST hold at most one authorised release at a time, a new `authorise` MUST replace the previous one with both logged, and an authorisation MUST lapse when its 10-minute install window expires.",
    },
    Requirement {
        id: "L-193",
        document: "docs/protocol/LINK.md",
        section: "Pairing reachability",
        statement: "A `PairingWindow` body MUST carry a non-zero `revision` and `remaining_ms` in 0..120000; its acknowledgement MUST carry a non-zero revision.",
    },
    Requirement {
        id: "L-194",
        document: "docs/protocol/LINK.md",
        section: "Pairing reachability",
        statement: "The comms processor MUST act on `PairingWindow` only from the controller UART, with session zero, after its own `LinkUp` has been validly answered under L-033.",
    },
    Requirement {
        id: "L-195",
        document: "docs/protocol/LINK.md",
        section: "Pairing reachability",
        statement: "The controller MUST send its current window state after linking, on the physical opening, and on every closure, including successful enrolment or reclaim and expiry.",
    },
    Requirement {
        id: "L-196",
        document: "docs/protocol/LINK.md",
        section: "Pairing reachability",
        statement: "The comms processor MUST retain only the greatest accepted revision and one local monotonic deadline.",
    },
    Requirement {
        id: "L-190",
        document: "docs/protocol/LINK.md",
        section: "The download window",
        statement: "Before it forwards any client frame, the comms processor MUST listen on the controller UART for `EnterDownload` for 1 500 ms from its own start, the half-open interval from the start to the start plus 1 500 ms, and MUST honour one arriving inside it: answer `entering`, set the ROM's force-download flag, and reset into the ROM within 100 ms of the answer, forwarding and answering nothing else meanwhile.",
    },
    Requirement {
        id: "L-191",
        document: "docs/protocol/LINK.md",
        section: "The download window",
        statement: "Outside that window the comms processor MUST answer `refused_outside_window` and MUST NOT set the flag; and whatever the window, it MUST NOT act on an `EnterDownload` that arrived on a client transport.",
    },
    Requirement {
        id: "L-192",
        document: "docs/protocol/LINK.md",
        section: "The download window",
        statement: "The controller MUST send `EnterDownload` only after it has itself reset the module, by cycling `EN` or the rail, MUST send the first within 200 ms of releasing `EN`, and MUST repeat it every 100 ms with the same `req_id` until it is answered or 3 000 ms have passed.",
    },
    Requirement {
        id: "L-180",
        document: "docs/protocol/LINK.md",
        section: "Error codes",
        statement: "Codes 257, 258 and 259 MUST reach the client whose frame raised them; the other six MUST NOT appear in a client-facing frame.",
    },
    Requirement {
        id: "L-181",
        document: "docs/protocol/LINK.md",
        section: "Error codes",
        statement: "An error raised about a link-local frame MUST carry `session_id = 0, req_id = 0` and MUST NOT be routed to a client.",
    },
    Requirement {
        id: "L-182",
        document: "docs/protocol/LINK.md",
        section: "Error codes",
        statement: "An error raised about a client's frame MUST be routed to that client, and MUST echo that frame's `session_id` and `req_id` when its envelope parsed, carrying `0, 0` only when it did not.",
    },
];
